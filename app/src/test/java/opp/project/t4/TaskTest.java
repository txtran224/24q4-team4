package opp.project.t4;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class TaskTest {
    /*
     * sort and prioritize tasks by deadline and type/progress
     * Represent individual tasks or items on the Kanban board, with fields like title, description, status, and possibly due date.
     */
}
